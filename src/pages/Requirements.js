import SectionPage from '../components/SectionPage';

function Requirements() {
  return (
    <SectionPage title="Requirements">
      <p>
        This section covers the functional and non-functional requirements of the project,
        including user needs, system constraints, and success criteria.
      </p>
    </SectionPage>
  );
}

export default Requirements;

import SectionPage from '../components/SectionPage';

function UIDesign() {
  return (
    <SectionPage title="UI Design">
      <p>
        This section describes the user interface design process, wireframes, mockups,
        and design rationale where applicable to the project.
      </p>
    </SectionPage>
  );
}

export default UIDesign;
